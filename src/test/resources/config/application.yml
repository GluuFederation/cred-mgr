# ===================================================================
# Spring Boot configuration.
#
# This configuration is used for unit/integration tests.
# ===================================================================

# ===================================================================
# Standard Spring Boot properties.
# Full reference is available at:
# http://docs.spring.io/spring-boot/docs/current/reference/html/common-application-properties.html
# ===================================================================

spring:
    application:
        name: credmgr
    datasource:
        url: jdbc:h2:mem:credmgr;DB_CLOSE_DELAY=-1
        name:
        username:
        password:
    jpa:
        database-platform: org.gluu.credmgr.domain.util.FixedH2Dialect
        database: H2
        open-in-view: false
        show_sql: true
        hibernate:
            ddl-auto: none
            naming-strategy: org.springframework.boot.orm.jpa.hibernate.SpringNamingStrategy
        properties:
            hibernate.cache.use_second_level_cache: false
            hibernate.cache.use_query_cache: false
            hibernate.generate_statistics: true
            hibernate.hbm2ddl.auto: validate
    mail:
        host: localhost
    mvc:
        favicon:
            enabled: false
    thymeleaf:
        mode: XHTML

liquibase:
    contexts: test

security:
    basic:
        enabled: false

server:
    port: 10344
    address: localhost

# ===================================================================
# JHipster specific properties
# ===================================================================

jhipster:
    async:
        corePoolSize: 2
        maxPoolSize: 50
        queueCapacity: 10000
    security:
        rememberMe:
            # security key (this key should be unique for your application, and kept secret)
            key: 24f59ab43edf21672b8f6f6e8a9d4bea7f99cee8
    metrics: # DropWizard Metrics configuration, used by MetricsConfiguration
        jmx.enabled: true
    swagger:
        title: credmgr API
        description: credmgr API documentation
        version: 0.0.1
        termsOfServiceUrl:
        contactName:
        contactUrl:
        contactEmail:
        license:
        licenseUrl:
        enabled: false
# ===================================================================
# cred-mgr specific properties
# ===================================================================
credmgr:
    gluuIdpOrg:
        host: https://gluu.localhost.info
        umaAatClientId: '@!0836.FE6E.EE3F.0ABF!0001!0843.BC87!0008!A9A6.BDC1'
        umaAatClientKeyId:
        clientId: '@!C0B0.0128.9A9D.9453!0001!EF7B.61C8!0008!189A.3F4A'
        requiredOpenIdScope: role_scopes
        requiredClaim: role
        requiredOPAdminClaimValue: IT Manager
        requiredOPSuperAdminClaimValue: Gluu IT Manager
